using System;

namespace mindbox
{
    class Program
    {
        static void Main(string[] args)
        {
            int radius = 5;

            findarea.area.findCircleArea(radius);

            int x = 1, y = 1, z = 1;
            findarea.area.findTriangleArea(x, y, z);
        }
    }
}
